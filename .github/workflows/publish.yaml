name: publish

on:
  push:
    branch:
      - 'master'

jobs:
  publish-site:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Setup git
        run: |
          git config --global user.email "mpippi@gmail.com"
          git config --global user.name "massi"
          git remote add upstream https://${{ secrets.GITHUB_TOKEN }}@github.com/masci/dev

      - name: Build site
        uses: jakejarvis/hugo-build-action@master
        with:
          args: -s site

      - name: Push to master
        run: |
          git add -A
          git commit -m"rebuilding site from ${{ github.sha }}"
          git push upstream master

      - name: Publish
        run: git push upstream `git subtree split --prefix=site/public`:gh-pages --force
